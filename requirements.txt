# Main pipeline env (amodal) — install torch separately via scripts/env.sh
# torch==2.13.0+cu132 / torchvision==0.28.0+cu132 from PyTorch cu132 index
#
# OpenAI CLIP is installed separately in scripts/env.sh with --no-build-isolation.
#
# HF stack lock (CRITICAL):
#   diffusers 0.39+ imports Dinov2WithRegistersConfig and needs transformers>=~4.57.
#   We pin diffusers 0.32.2 + transformers 4.46.3 — enough for SD2 inpaint and
#   safer for GroundingDINO / RAM loaders used by this pipeline.

numpy>=1.26.0
tqdm>=4.66.0
matplotlib>=3.8.0
pandas>=2.1.0
scikit-image>=0.22.0
scikit-learn>=1.3.0
opencv-python>=4.8.0
Pillow>=10.0.0
PyYAML>=6.0.1
omegaconf>=2.3.0
easydict>=1.13
hydra-core>=1.3.0
packaging>=24.0
joblib>=1.3.0
albumentations>=1.3.0
tabulate>=0.9.0
requests>=2.31.0
safetensors>=0.4.0
huggingface_hub>=0.26.0,<0.30.0
diffusers==0.32.2
transformers==4.46.3
accelerate==1.2.1
tokenizers>=0.19,<0.21
gradio==4.44.1
gradio_client==1.3.0
# pydantic>=2.11 emits additionalProperties:true (bool) → Gradio 4.44 API schema crash
pydantic>=2.0,<2.11
kornia>=0.7.0
scipy>=1.11.0
timm>=0.9.0
ftfy>=6.1.0
regex>=2023.0.0
setuptools>=68.0.0,<81
wheel
# Sibling runtime deps (GroundingDINO / InstaOrder import these at module load)
addict>=2.4.0
yapf>=0.40.0
pycocotools>=2.0.6
supervision>=0.22.0
